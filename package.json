{
  "name": "tasklist",
  "version": "1.0.0",
  "description": "An application to maintain a list of To-Do items and store it in a Mongo database which is connected to the Node server in the backend which in turn serves the Angular front end to show all the data stored in the database",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anurag Dutta",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "concurrently": "^4.1.0",
    "cors": "^2.8.5",
    "express": "^4.16.4",
    "mongojs": "^2.6.0",
    "mongoose": "^5.4.14",
    "nodemon": "^1.18.10"
  }
}
